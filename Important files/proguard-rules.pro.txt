# ==========================
# NUKKAD Project ProGuard Rules
# ==========================

# Keep all WebRTC classes (remote video/audio)
-keep class org.webrtc.** { *; }

# Keep your app's WebRTC wrapper classes
-keep class com.nukkad.** { *; }

# Keep all annotations (required for some libraries)
-keepattributes *Annotation*

# Keep native methods (used by WebRTC)
-keepclasseswithmembernames class * {
    native <methods>;
}

# Keep Firebase classes
-keep class com.google.firebase.** { *; }
-keep class com.google.android.gms.** { *; }

# Keep Gson models (if using Gson)
-keep class com.google.gson.** { *; }

# Keep logging (optional, helps debug)
-keep class org.webrtc.Logging { *; }

# Keep all Parcelable classes (Android serialization)
-keepclassmembers class * implements android.os.Parcelable {
    public static final android.os.Parcelable$Creator *;
}

# Keep RxJava and Kotlin classes (if used)
-keep class io.reactivex.** { *; }
-keep class kotlin.** { *; }

# Keep methods with @Keep annotation
-keep @androidx.annotation.Keep class * { *; }

# ==========================
# Optional: Add any other library-specific rules here
# ==========================
